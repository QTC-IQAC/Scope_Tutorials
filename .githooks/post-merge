#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)

if command -v python3 >/dev/null 2>&1; then
    python3 "$repo_root/scripts/export_tutorials.py"
else
    echo "python3 not found; skipping tutorial export." >&2
fi
